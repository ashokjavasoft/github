package com.comparable;

public class EmpComparable implements Comparable<Empcls> {

	@Override
	public int compareTo(Empcls o) {
		return 0;
	/*	//Empcls e=new Empcls();
		if(this.age ==	Empcls.getAge())
			return 0;
		else if (equals(e.age >	e.getAge()))
			return 1;
		else
			return -1;
			}*/
	}

}
